import React from 'react';
// Ensure the path and file exist; update the path if needed
// Ensure the path and file exist; update the path if needed
import AppNavigator from "../navigation/AppNavigator"; // Update this path if the file is elsewhere, e.g. './AppNavigator' or '../../navigation/AppNavigator'


export default function HomeTab() {
  return <AppNavigator />;
}